from __future__ import annotations


def maybe_transliterate(text: str, target_script: str | None = None) -> str:
    """Transliterate romanized text to a target script when applicable.

    Placeholder implementation. Replace with a real transliteration engine.
    """
    return text
